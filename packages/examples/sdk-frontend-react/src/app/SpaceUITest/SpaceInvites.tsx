import { useSpaceComponents } from './useSpaceComponents';

export const SpaceInvitesComponent = () => {
  const { SpaceInvitesComponent } = useSpaceComponents();

  return <SpaceInvitesComponent />;
};
