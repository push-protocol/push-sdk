{
  "name": "boilerplate",
  "version": "1.0.0",
  "description": "Example boilerplate for testing various features of Push Protocol",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "nodemon",
    "inspect": "nodemon --inspect index.js",
    "dev": "node-dev --respawn --transpile-only index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pushprotocol/restapi": "latest",
    "ethers": "5.7.2",
    "input": "1.0.1"
  },
  "devDependencies": {
    "nodemon": "3.0.1"
  }
}
