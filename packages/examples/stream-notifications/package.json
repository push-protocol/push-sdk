{
  "name": "stream-notifications",
  "version": "1.0.0",
  "description": "Example of Push Notifications streaming notifications using the Push Protocol",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "nodemon",
    "inspect": "nodemon --inspect index.js",
    "dev": "node-dev --respawn --transpile-only index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pushprotocol/restapi": "1.4.35",
    "ethers": "5.7.2",
    "input": "1.0.1"
  },
  "devDependencies": {
    "nodemon": "3.0.1"
  }
}
